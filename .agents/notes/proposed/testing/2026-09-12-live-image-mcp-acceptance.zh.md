# 真实图像 MCP 与多图设计验收待办

Status: proposed
Translation: pending

## 摘要

用户要求真实测试包含图像生成与编辑，并提供 AruHub image2 的测试连接。正常安装包已通过 Folio MCP 发起三次真实生成和一次真实编辑，得到四张图片，最终四图均被原生 Kimi 读取；其中两次生成原回合的工具回执未完成，不能算完整成功。真实图像测试还发现了 MCP 客户端默认超时短于服务超时的问题，修复及原失败分别留证。多图保存导出和人工视觉验收仍待完成；海报 PNG/JPEG 导出在修正构建上有逐格式通过证据（见末节），信息图/长图负载与人工评定仍待完成；模拟测试继续验证确定性协议和错误路径。

## 测试连接

| 字段         | 本次测试配置                                                     |
| ------------ | ---------------------------------------------------------------- |
| 供应商名称   | AruHub image2                                                    |
| API base URL | `https://direct.aruhub.com:8443/v1`                              |
| model        | `gpt-image-2.5-sunburst`                                         |
| API key      | 用户已提供；执行测试时通过现有图像连接的机密配置使用，不写入仓库 |
| 官网         | 未提供，不从 API 地址推定                                        |

这是用户提供的测试连接，不是 Folio 产品默认供应商或模型；URL、Key、model 仍由用户配置。协议与支持参数按真实响应核验，不因模型名推断特性。请求必须通过 Folio 内置 image MCP 和已有连接/素材保存链路；直接 HTTP 调通只能作为诊断，不能替代 MCP 验收。

最初新增此待办时仅整理文档，没有执行 API 请求。用户随后恢复整体实施 goal，实际测试使用该连接时，保留显式模型，不自动换供应商或模型，不对失败请求静默重试。使用自建测试提示词和测试图片，不上传用户工作文件；执行记录排除密钥、Authorization、带凭据的 URL 和完整会话转录。

## 必做 TODO

- [ ] **真实安装包连接与 MCP 入口。** 固定实际安装包/产品源、操作系统及用户选择的 Agent/runtime/ACP，使用已有设置保存上述连接。记录真实暴露的 image generate/edit 工具和调用路径，确认 model 原样发送、没有产品默认值或模拟响应替代。机密配置缺失时明确记录阻塞，不回退假数据并标通过。
- [ ] **真实 generate。** 通过 Folio MCP 生成至少 3 张不同的测试素材，例如产品主体、场景背景和装饰插图；保存返回的真实图片字节，核对 MIME、可解码性、尺寸及素材路径。实际打开图片，不能只看 HTTP 200、返回 URL 或模型的成功描述。该最小样本供后续多图画稿复用，避免每个场景重复生成同一素材。
- [ ] **真实 edit。** 使用上述一张真实输出作为输入，通过 Folio MCP 至少完成一次有明确可视变化的编辑，例如保持产品形状和构图、把蓝色主体改成红色。核对传入的原图与请求关联、返回的新素材及原图保留，人工比较是否完成目标且主体可辨；不能以另一张无输入的 generate 冒充 edit。多参考图和 mask 按拟宣称支持的实际组合补测；不支持时记录限制，不静默改协议或放宽结论。
- [ ] **生成/编辑 → Agent 读图 → PPTD/Bento。** 使用一个实际支持 Folio MCP 的已配置 Agent 完成调用与真实图片读取，把生成素材用于可编辑画稿；再从选中图片执行编辑并替换目标。确认图片工具本身不提前提交画稿，最终图片引用与确切字节一致，无关元素和旧素材保持正确。Provider 接通、Agent 真正获得图片输入、视觉质量分别记录。其他 Agent 的发现/传输/读图仍归五 Agent 矩阵，不能凭一个 Agent 通过替它们盖章。
- [ ] **真实图片负载的保存、恢复与导出。** 至少包含一份 12 个图片元素的多图长图或信息图，以及一份使用真实主体/背景图片的海报。记录独立素材数、原始尺寸及总字节数，至少使用上述 3 张不同真实生成图和编辑输出；复用相同图片时如实记录，不以微型占位图充数。验证裁切、缩放、替换、撤销/重做、自动保存、退出重开、PPTD 往返和 PNG/JPEG 导出，无丢图、错图或失效素材路径。测量加载、编辑、保存、导出与峰值内存，供人工判断目标机器可接受性；不预设未经验证的性能通过阈值。
- [ ] **失败与视觉结论归档。** 真实服务出现失败、超时、权限或不支持参数时保留错误事实和已有素材/画稿，不用重复付费请求掩盖失败。故意制造的 HTTP/超时/损坏图/取消等负例优先复用现有模拟设施，不为异常覆盖反复调用真实服务。汇总每次真实调用数量、耗时、响应类型、输出尺寸/摘要及可观测用量/费用，未知费用标未知。人工分别评定生图可用性、编辑目标达成和多图版式/交互体验，不能由自动检查代填。

测试用例的固定输入/断言只约束验收，不写入产品 skill 来强制 Agent 按固定创作步骤工作。图像生成具有不确定性，真实测试不按图片逐像素相等判断成功；技术完整性和视觉判断分别报告。

## 关联实施与完成条件

- [主计划 P6.7](../architecture/2026-09-09-graphic-design-platform.zh.md)承接真实图像服务和多图场景验收，补充原 P6.2/P6.4，不创建新的测试引擎或图像作业系统。
- [T28 安装包矩阵](2026-09-11-installed-five-agent-matrix.md)记录真实服务与具体 Agent/MCP 组合；已有模拟 generate/edit 的通过记录保留，不能更名为真实供应商通过。
- [T29 完整设计验收](2026-09-11-complete-design-acceptance.md)承接真实多图画稿、保存/导出及人工视觉/编辑结论。既有少量小图的性能测量不覆盖这一负载。
- Git 版本功能交付后，将本次真实素材样稿用于 P6.6 历史恢复与素材留存验证；真实 generate/edit 基础验收不等待 Git 版本功能上线。

只有真实 generate、真实 edit、素材进入设计后的保存/重开/导出和人工检查各自有结果，才可关闭这一待办。某项供应商不支持、MCP 未暴露、调用失败或人工未评定时保持该项未完成；模拟测试或打包成功不能替代。尚未执行的状态保持未勾选，不宣称全部 Agent 或全部平台已通过。

最初文档变更未运行真实模型、图像服务、产品测试或构建；以下记录后续执行，文档验证本身不构成图像服务验收。

## 首轮实际执行（2026-09-12，部分结果）

正常 macOS arm64 私有安装包来自 `50ddd41bfb3e88537c0dcbec40c0ce86ca40b8e4`，
构建、DMG 校验及安装身份已验证。通过原生 Kimi 和 Folio 内置 MCP、已有设置保存
用户指定连接；语言模型使用隔离合成服务，图像请求直接发送到用户指定 AruHub。
先行合成轮 `folio-t28-kimi-input-AfwzY3` 完成三次 generate、一次 edit 和四次
原生读图（handle 67079 exit 0），不把此轮计为真实图片验收。

真实轮 `folio-t28-kimi-input-1tmh3w`（handle 85875 exit 1）已有以下结果：

- 第一张通过实际 generate 返回，PNG 为 1024×1536、2,259,449 字节，约 36.1 秒；
  SHA256 `17c17e844c705badf07c42dad425c4f16d0ee283587866c7ce0c3573049d10f6`。
  紧随其后的原生 `ReadMediaFile` 将相同图片字节送入模型请求。已实际打开图片，
  可见绿叶与浅色纸面留白；这一观察不代替用户的视觉质量评定。
- 第二次 generate 已经原生单次批准，随后探针退出。约 27 秒后同会话目录留下
  第二张 PNG，1,460,148 字节，SHA256
  `9fdd23bae6bcda97594913d29275bdcf4b03f777589b826f072c9f3a39115e2d`。
  该文件已复制到同轮 evidence 的 `recovered-asset-2.png`；它没有被探针接收为
  对应工具结果，也没有该轮原生读图证据，只记为恢复产物，不能声称第二项完整通过。
- 第三次 generate 和 edit 未执行。没有自动重试、切换模型或重新生成已有素材；
  后续优先复用已保存图片，先以合成服务查明探针异常再继续未执行操作。
- 关闭桌面时一个所属 MCP 进程暂未退出，导致 harness 保留隔离数据；该进程随后
  自行退出。该事实及失败保留，不把未清理的进程视为成功关闭。

证据保存在系统临时目录下上述轮次的 `evidence/`：第一项工具结果、对应原生读图
证明、图片摘要和 `failure.json`。日志位于私有安装轮 `folio-current-50ddd41b-78c0b9vz`。
Key 只写入隔离设置，trace 在输入前丢弃，文本诊断脱敏，不把含 Key 的配置或完整
会话转录写入仓库。首轮错误消息脱敏时丢失了具体原因，尚不归因为供应商或产品错误。
实际 HTTP 状态和费用没有被独立捕获，费用保持未知。

合成诊断轮 `folio-t28-kimi-input-kkq4pz`（handle 49188 exit 0）复用真实大图作为
合成响应，明确持有第二次图像响应 41.1 秒，原控制流程未复现错误；四次工具和读图
均完成，所属进程与端点清理通过。此结果不能解释原失败，也不构成真实供应商通过。

## 后续生成、超时与真实编辑（2026-09-12）

继续复用上述两张图片，没有重新生成。第三次真实 generate 位于
`folio-t28-kimi-input-CmIQPg`（handle 43037 exit 1）：05:07:42Z 获批后，
探针的全局权限处理器重复进入已经消费的批准卡，等待消失的标题而失败。这是探针故障；
修正为按会话及工具调用逐项处理权限，不扩大许可范围。失败清理继续等待已批准请求，
没有再次发起图像调用，保留了另一个独立的产品故障：Kimi 在 60 秒返回
`MCP error -32001: Request timed out`，而服务请求继续至约 74 秒并保存了真实 PNG。
第三张为 1024×1536、3,283,105 字节，SHA256
`60ca177f79b22e782c4baf5b9eb542eecd26999f9e83a2be798dfb98ade30aea`，
保存在该轮 `evidence/recovered-asset-3.png`。恢复字节不把失败回执改为成功。

Folio 图像 HTTP 原有上限为 180 秒，Kimi 和 Pi MCP SDK 默认仅等 60 秒。
[客户端期限修复](../../implemented/bug-fix/2026-09-12-image-mcp-client-deadline.md)
让 Pi 图像调用等待 210 秒，Kimi 设计会话通过公开配置补充默认值，尊重用户配置；
没有改托管 runtime、加付费重试或扩大服务期限。源码检查已通过，新安装包默认值回归另行验收。

最后一轮 `folio-t28-kimi-input-uO0aN7`（handle 68769 exit 0）仍使用
`50ddd41` 正常安装包，在隔离的 Agent 设置中显式配置公开
`KIMI_MCP_TOOL_TIMEOUT_MS=210000`。该轮先复用并实际读取三张已有图片，
仅新发起一次 edit：以第一张绿叶图为输入，要求保留构图与文字留白，把叶片改为铜色和琥珀色。
47.7 秒返回 1024×1536、2,146,871 字节 PNG，SHA256
`3cc4942c6f551cf7eaf93edff1a99afb42fe7f7e5282545abd5968c54a36057f`。
已实际打开编辑图，观察到原构图和白花保留、叶片变为铜色；这只是技术验收的观察，
用户对质量和目标达成的判定仍待进行。没有把无输入的 generate 计为 edit。

最终 `evidence/assets-manifest.json` 记录四图确切摘要、编辑输入关联和四次原生读图证据；
图片为 `asset-1.png` 至 `asset-4.png`。原生 `ReadMediaFile` 的图片字节进入隔离合成语言
模型请求，因此证明工具和读图链路，不证明真实语言模型自主创作或语义审美能力。
第二、三张此轮读图成功，与各自原回合工具回执未完成是不同结论。
这一轮端点、所属进程和隔离数据清理通过；此前保留的含连接密钥测试配置在确认进程退出后清除。

累计真实请求为 **3 generate + 1 edit，无重试**。HTTP 状态、服务用量和费用没有独立捕获，
仍标未知。四张素材交给完整设计验收复用，不为填补历史回执再付费生成。
多图原生编辑、自动保存、PPTD、导出、Git 素材恢复以及用户视觉评价未完成，以上 TODO 不整体勾选。

## 修正轮导出验证（2026-09-12，新构建 47c0808）

50ddd 包的逐格式真相：诊断 12327 对同一海报画稿交替导出 PNG/JPEG 各三次，
导出 0、1、4（角像素 `[242,246,247]`）正确，导出 2、3、5（角像素 `[44,28,16]`）
为启动画面错帧；同期 DOM 六次全部正确（单个 `.bento-slide`、正确作品文本、无
`#bento-splash`），故障在 `capturePage` 拿到过期合成帧，不在元素选择或编码。
根因与最小修复见 [导出捕获验证](../../implemented/bug-fix/2026-09-12-export-capture-frame-verification.md)；
attach/leave 竞态修复见 [离开等待加载](../../implemented/bug-fix/2026-09-12-leave-attach-loading-race.md)。
两个修复与 T28 取消修复（`89d9b07` → `16a5940`）同入新构建
`47c0808d354ce2227e4fed288a748f4fec9e2435`（DMG SHA256
`f50900870ca1442feee70f6f391e633e6ef330420935ec8a1875a09e11300a62`、asar SHA256
`6925f94faae19bfc002973a50cf5c527b05a42363a990e7a7d2ebe6a5781ce0a`，身份文件见
`folio-current-47c0808d-6nzcp7xj/package-identity.json`）。

新包安装态验收（串行，复用上述四图字节，无新增付费调用）：

- 双格式导出：同一海报新会话导入 → 快照一致 → 保存 → 回读一致 → 交替 PNG/JPEG
  六次，角像素六次全部正确；两种格式各抽一文件读图，均为完整正确海报
  （标题、四张真实图、日期），且同格式重复导出字节一致。证据
  `/tmp/folio-export-repro-oowgDi/evidence`（含 `result.json` 与六个导出文件）。
  旧包逐格式失败保留为历史，不覆盖。
- 可编辑状态检查：同上流程覆盖新会话导入 → 快照 → 保存 → 回读一致，证明本进程内
  导入的 Bento 快照可保存并从存储回读；该轮没有退出应用、启动新进程或复制 profile，
  因此不构成可编辑副本重开验证。新进程重开仍待执行。
- 合成 Pi 取消：`native-image-cancel-passed`，被 hold 的合成图像 HTTP 在真实 UI
  Stop 后、fixture 清理前关闭（旧包失败为清理时才关闭）。证据
  `/tmp/folio-t28-kimi-input-EeOUGj/evidence`。无付费调用。
- leave 回归：新会话 attach 周期内 `design.leave` 三次全部 true 且零未保存对话框，
  但三轮 `exercised:false`，只证明常规 ready 实例的 leave guard 未回归，没有再次命中
  原始 loading attach 的短竞态窗口。结构性修复证据仍由原失败记录与确定性 core 测试承担。
  证据 `/tmp/folio-leave-repro-nByRB0/evidence`。
- Grok 旧探针在 47c0808 与 50ddd 包均未进入 provider 请求，但这不能排除产品回归：
  后续静态核验发现旧隔离守卫会拒绝安装包工作目录下的能力探测，且脚本提前关闭了
  尚未完成验证的配置对话框。修正夹具后的 47c0808 独立轮次
  `folio-installed-grok-stop-v2-S6FMNQ` 已通过能力探测、原生启动及会话创建，
  随后的 `x.ai/hooks/action` 重载请求返回 `-32601 Method not found`，仍未进入
  provider 请求或 Stop/显式恢复。该轮退出 1，清理通过；适配边界由 T20 继续诊断，
  旧失败记录保留，脚本文件名中的 UNEXECUTED 不作为实际执行状态。

人工视觉/编辑体验与目标机器容忍度仍待用户评定；以上“读图正确”仅为代理观察，
不代替人工结论。本轮无新增真实 generate/edit，累计仍为 3 generate + 1 edit。
信息图/长图负载与 Save version 链路仍待覆盖，本 TODO 不整体勾选。
